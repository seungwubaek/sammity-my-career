'use client';

import React from 'react';
import styled from 'styled-components';

export const StDivLinkSet = styled.div`
  display: flex;
  gap: 1rem;
  a {
    display: flex;
    align-items: center;
  }
`;
