'use client';

import React from 'react';
import styled from 'styled-components';

export const StDivCareerCardWrapper = styled.div`
  display: flex;
  justify-content: space-evenly;
  flex-wrap: wrap;
`;
