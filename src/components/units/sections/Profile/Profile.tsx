import React from 'react';
import { StSection, SectionTitle } from '@/components/units/sections/Section';

const SectionProfile: React.FC = () => {
  return (
    <StSection>
      <SectionTitle>Profile</SectionTitle>
    </StSection>
  );
};

export default SectionProfile;
