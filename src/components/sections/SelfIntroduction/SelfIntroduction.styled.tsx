'use client';

import React from 'react';
import styled from 'styled-components';

import { StSection } from '@/components/sections/SectionBase';

export const StDivSpeechCardContainer = styled.div`
  margin: 20px auto;
`;
