'use client';

import React from 'react';
import styled from 'styled-components';

export const StDivSpeechCard = styled.div`
  padding: 20px;
  border: 1px solid ${({ theme }) => theme.color.speechCardBorder};
  border-radius: 15px;
`;
